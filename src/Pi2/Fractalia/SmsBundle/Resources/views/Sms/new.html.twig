{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Sms creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('sms') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
