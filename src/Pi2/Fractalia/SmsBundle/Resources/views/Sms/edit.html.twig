{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Sms edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('sms') }}">
            Volver al listado de Sms
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
