{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Mensaje list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Templatename</th>
                <th>Texto</th>
                <th>Estadoconstruccion</th>
                <th>Bitacora</th>
                <th>Fechacreacion</th>
                <th>Fechaactualizacion</th>
                <th>Fechainsercionsms</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('mensaje_show', { 'id': entity.id }) }}">{{ entity.templatename }}</a></td>
                <td>{{ entity.texto }}</td>
                <td>{{ entity.estadoConstruccion }}</td>
                <td>{{ entity.bitacora }}</td>
                <td>{% if entity.fechaCreacion %}{{ entity.fechaCreacion|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.fechaActualizacion %}{{ entity.fechaActualizacion|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.fechaInsercionSms %}{{ entity.fechaInsercionSms|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('mensaje_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('mensaje_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('mensaje_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
