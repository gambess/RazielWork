{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Columnaresumen</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Numerocaso</th>
                <td>{{ entity.numeroCaso }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ entity.estado }}</td>
            </tr>
            <tr>
                <th>Servicio</th>
                <td>{{ entity.servicio }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('columnaresumen') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('columnaresumen_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
