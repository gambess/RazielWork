{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Columnaresumen list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numerocaso</th>
                <th>Estado</th>
                <th>Servicio</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('columnaresumen_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.numeroCaso }}</td>
                <td>{{ entity.estado }}</td>
                <td>{{ entity.servicio }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('columnaresumen_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('columnaresumen_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('columnaresumen_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
